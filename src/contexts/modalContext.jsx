import React, {useState, con} from 'react'

const modalContext = () => {
  const [showModal, setShowModal] = useState(false);

  return (
    <div>modalContext</div>
  )
}

export default modalContext